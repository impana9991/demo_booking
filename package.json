{
  "name": "demo-booking",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "dev": "concurrently -n api,web -c teal,magenta \"npm run start --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run dev",
    "dev:backend": "npm run start --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
